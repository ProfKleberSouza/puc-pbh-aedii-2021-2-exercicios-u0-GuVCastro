#include <stdio.h>

int main() {
	int consumo, tempo, rendimento, velocidade;
	printf("Insira entre espaços o tempo percorrido, velocidade e rendimento médio do veículo: ");
	scanf("%i %i %i", &tempo, &rendimento, &velocidade);
	consumo = (tempo/velocidade)/rendimento
	printf("SOMA = %i\n", n1+n2);
	return 0;
}
